import path from "path"

/**
 * Resolves the folder where the template with the provided name is located.
 * @param templateName
 */
export const resolveTemplate = (templateName: string, rootFolder: string = "./") => {
	return path.resolve("./")
}
